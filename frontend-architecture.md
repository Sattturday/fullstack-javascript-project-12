# Архитектура проекта Fullstack JavaScript Project 12

## Общая структура проекта

Проект представляет собой fullstack JavaScript приложение с клиент-серверной архитектурой. Основная структура проекта включает:

```
.
├── Makefile
├── package.json (корневой)
├── frontend/
│   ├── package.json
│   ├── index.html
│   ├── vite.config.js
│   ├── public/
│   │   └── favicon.svg
│   └── src/
│       ├── index.jsx (точка входа)
│       ├── init.jsx (инициализация приложения)
│       ├── App.jsx (основной компонент приложения)
│       ├── routes.js (маршруты приложения)
│       ├── components/
│       │   ├── Header.jsx
│       │   ├── AuthPageLayout.jsx
│       │   ├── ChannelsList.jsx
│       │   ├── ChatWindow.jsx
│       │   ├── ProtectedRoute.jsx
│       │   └── modals/
│       │       ├── AddChannelModal.jsx
│       │       ├── RemoveChannelModal.jsx
│       │       └── RenameChannelModal.jsx
│       ├── pages/
│       │   ├── Home.jsx
│       │   ├── Login.jsx
│       │   ├── Signup.jsx
│       │   └── NotFound.jsx
│       ├── services/
│       │   ├── api.js
│       │   ├── createI18n.js
│       │   └── socket.js
│       ├── contexts/
│       │   └── SocketContext.js
│       ├── hooks/
│       │   └── useAuth.js
│       ├── store/
│       │   ├── index.js
│       │   ├── authSlice.js
│       │   ├── uiSlice.js
│       │   └── socketMiddleware.js
│       └── validation/
│           ├── loginSchema.js
│           ├── signupSchema.js
│           └── channelSchema.js
└── node_modules/ (игнорируются)
```

## Фронтенд архитектура

### Точка входа и основное приложение

- `frontend/src/index.jsx` - точка входа приложения, подключает React и основной компонент App
- `frontend/src/init.jsx` - инициализация приложения, создание store, i18n и socket
- `frontend/src/App.jsx` - основной компонент приложения, содержит маршрутизацию
- `frontend/src/routes.js` - централизованное определение маршрутов приложения

### Маршрутизация

Приложение использует react-router-dom для навигации между страницами:

- `/` - главная страница (защищенный маршрут)
- `/login` - страница аутентификации
- `/signup` - страница регистрации
- `*` - страница 404 (не найдено)

Маршруты определены в файле `routes.js` и используются в компоненте `App.jsx`.

### Компоненты

- `components/Header.jsx` - компонент заголовка приложения с кнопкой выхода
- `components/ProtectedRoute.jsx` - компонент для защиты маршрутов, требующих аутентификации
- `components/ChannelsList.jsx` - компонент для отображения списка каналов
- `components/ChatWindow.jsx` - компонент для отображения окна чата
- `components/modals/Modals.jsx` - компонент для управления модальными окнами
- `components/modals/AddChannelModal.jsx` - модальное окно для добавления канала
- `components/modals/RemoveChannelModal.jsx` - модальное окно для удаления канала
- `components/modals/RenameChannelModal.jsx` - модальное окно для переименования канала

### Страницы

- `pages/Home.jsx` - главная страница приложения
- `pages/Login.jsx` - страница входа в систему
- `pages/Signup.jsx` - страница регистрации
- `pages/NotFound.jsx` - страница для несуществующих маршрутов

### Структура модальных окон

Модальные окна управляются централизованно через компонент `Modals.jsx`, который:
- Отображает соответствующее модальное окно в зависимости от типа
- Передает необходимые данные в модальные окна
- Обрабатывает закрытие модальных окон

Поддерживаемые типы модальных окон:
- `add` - модальное окно добавления канала
- `remove` - модальное окно удаления канала
- `rename` - модальное окно переименования канала

### Состояние приложения (Redux)

Приложение использует Redux Toolkit для управления состоянием:

- `store/index.js` - конфигурация хранилища Redux
- `store/authSlice.js` - состояние аутентификации
- `store/uiSlice.js` - состояние пользовательского интерфейса
- `store/socketMiddleware.js` - middleware для обработки WebSocket событий

### Сервисы

- `services/api.js` - конфигурация API взаимодействия с использованием RTK Query
- `services/createI18n.js` - конфигурация интернационализации с использованием i18next
- `services/socket.js` - конфигурация WebSocket соединения с использованием Socket.IO

### Контексты

- `contexts/SocketContext.js` - контекст для предоставления WebSocket соединения компонентам

### Хуки

- `hooks/useAuth.js` - пользовательский хук для управления аутентификацией

### Валидация

- `validation/loginSchema.js` - схема валидации для формы входа
- `validation/signupSchema.js` - схема валидации для формы регистрации
- `validation/channelSchema.js` - схема валидации для форм работы с каналами

## Технологии

### Основные библиотеки и фреймворки

- React 19 - основной фреймворк для построения пользовательского интерфейса
- Redux Toolkit - управление состоянием приложения
- React Router v7 - маршрутизация в приложении
- RTK Query - автоматическая генерация хуков для работы с API
- Socket.IO Client - для работы с WebSocket соединениями
- Bootstrap 5 - CSS фреймворк для стилизации
- Formik - управление формами
- Yup - валидация форм
- i18next - интернационализация
- react-toastify - уведомления
- leo-profanity - фильтрация ненормативной лексики
- Rollbar - отслеживание ошибок (опционально)
- React Bootstrap - компоненты для React на основе Bootstrap

### Инструменты разработки

- Vite - инструмент сборки и разработки
- ESLint - линтинг JavaScript кода
- PropTypes - проверка типов в React компонентах

## Архитектурные особенности

1. **Многослойная архитектура**: разделение на компоненты, страницы, сервисы, контексты, хуки, хранилище и валидацию
2. **Защита маршрутов**: реализована через компонент ProtectedRoute
3. **Централизованное состояние**: все состояние приложения управляется через Redux
4. **Модульная структура**: код организован по функциональным модулям
5. **Реактивность**: использование современных возможностей React и Redux
6. **RTK Query**: для работы с API и автоматической генерацией хуков
7. **WebSocket**: для получения обновлений в реальном времени
8. **Middleware**: для обработки WebSocket событий в Redux
9. **Контекст**: для предоставления WebSocket соединения компонентам
10. **Пользовательские хуки**: для инкапсуляции бизнес-логики
11. **Валидация**: централизованная валидация форм с локализацией
12. **Обработка ошибок**: централизованная обработка сетевых ошибок и ошибок аутентификации
